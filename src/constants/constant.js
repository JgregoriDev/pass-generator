export const lowercaseLetters = Array.from({ length: 26 }, (_, index) => String.fromCharCode(97 + index));
export const uppercaseLetters = Array.from({ length: 26 }, (_, index) => String.fromCharCode(65 + index));
export const numbersArray = Array.from({ length: 10 }, (_, index) => String(index));
export const symbolsArray = ['!', '@', '#', '$', '%', '&'];

